# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
firebase-debug.log*
firebase-debug.*.log*

# Firebase cache
.firebase/

# Firebase config

# Uncomment this if you'd like others to create their own Firebase project.
# For a team working on the same Firebase project(s), it is recommended to leave
# it commented so all members can deploy to the same project(s) in .firebaserc.
# .firebaserc

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Directory for instrumented libs generated by jscoverage/JSCover
lib-cov

# Coverage directory used by tools like istanbul
coverage

# nyc test coverage
.nyc_output

# Grunt intermediate storage (http://gruntjs.com/creating-plugins#storing-task-files)
.grunt

# Bower dependency directory (https://bower.io/)
bower_components

# node-waf configuration
.lock-wscript

# Compiled binary addons (http://nodejs.org/api/addons.html)
build/Release

# Dependency directories
node_modules/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env

# dataconnect generated files
.dataconnect

## 🔒 **Enhanced .gitignore for Smart Home Project**

### **New Security-Related Entries:**
- **Biometric files**: `biometric-hashes.json`, `webauthn-credentials.json`
- **Security certificates**: `*.key`, `*.pem`, `*.crt`, `*.cert`
- **Environment secrets**: `*.secret`, `secrets/`, `.env.secret`

### **Supabase & Database:**
- **Supabase local files**: `.supabase/`, `supabase/.temp/`
- **Prisma migrations**: `prisma/migrations/`, `prisma/dev.db`
- **Additional env patterns**: `.env.*.local`, `.env.staging`

### **AI/ML & Sensor Data:**
- **Model files**: `models/`, `face-api-models/`, `tensorflow-models/`
- **Sensor data**: `sensor-data/`, `sensor-logs/`, `*.sensor`
- **Large model files**: `*.weights`, `*.bin`, `*.shard*`

### **Media & Large Files:**
- **Additional video formats**: `*.3gp`, `*.ogv`
- **Additional audio formats**: `*.aac`, `*.ogg`, `*.wma`
- **Additional image formats**: `*.bmp`, `*.raw`, `*.cr2`, `*.nef`

### **Development & IDE:**
- **VSCode settings**: `.vscode/settings.json`, `.vscode/launch.json`
- **Additional IDE files**: `*.sublime-project`, `.atom/`
- **OS files**: `$RECYCLE.BIN/`, `*.lnk`

### **Build & Cache:**
- **Generated files**: `generated/`, `auto-generated/`
- **Documentation builds**: `docs/build/`
- **Additional cache patterns**: `.jest/`, `dev/`

## 🎯 **Key Benefits:**

1. **Security**: Prevents accidental commit of sensitive data
2. **Performance**: Excludes large model files and media
3. **Cleanliness**: Keeps repository focused on source code
4. **Team Safety**: Prevents environment-specific files from being shared

## 📝 **Optional Considerations:**

You might want to **uncomment** these lines if you prefer to commit lock files:
```gitignore
<code_block_to_apply_from>
```

This enhanced `.gitignore` will help keep your repository clean and secure while working with biometric authentication, AI models, and sensor data in your smart home project!
